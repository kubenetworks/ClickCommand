import Box from '@/components/Box';

export default function Dashboard() {
  return (
    <main className="">
      <Box
        className="pad dpfx justifyCenter alignCenter fs16 fw500"
        style={{ height: 'calc(100vh - 100px)' }}
      >
        Dashboard
      </Box>
    </main>
  );
}
